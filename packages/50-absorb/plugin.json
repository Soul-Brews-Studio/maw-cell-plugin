{
  "name": "absorb",
  "version": "0.1.0",
  "weight": 50,
  "entry": "./index.ts",
  "sdk": "^1.0.0",
  "description": "Absorb an oracle — retire + rsync knowledge to central vault",
  "cli": {
    "command": "absorb",
    "help": "maw absorb <oracle-name> --confirm [--reason <r>] [--dry-run] [--json]",
    "flags": {
      "--confirm": "boolean",
      "--reason": "string",
      "--dry-run": "boolean",
      "--json": "boolean"
    }
  }
}
